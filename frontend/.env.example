# Copy to .env. Do not commit .env.
# Dev: use /api so Vite proxies to backend.
# Prod (Vercel): set VITE_API_URL to your Render backend URL (e.g. https://agentic-workflow-builder-api.onrender.com)

VITE_API_URL=/api
